MONGODB_URI=
REDIS_URL=
CORS_ORIGIN=
RANGO_VISIBILIDAD=6

# Production uses the managed MongoDB Atlas integration in Vercel.
# User activity endpoints are available at /posts/user/:userId and /comments/user/:userId.
